# P619 night-lighting note state

## Root guard

```text
cwd=C:\sgSHIOK2026
hostname=PRAWN-E14
```

## Scope

```text
Free-tier web UI note change. No scoring, export, rescore, subset run, ingest, network build, upstream API probe, input mutation, protected QA mutation, public-data write, deployment, or locked-weight change was performed.
```

## Change

```text
The night-lighting layer note is now state-aware. When the layer is off it says "Switch on and zoom into a neighbourhood to load lamp-post points." When the layer is on it says "Zoom into a neighbourhood to load lamp-post points." The text is generated by the exported nightLightingLayerNote() helper and the page uses that helper beside the stateful toggle.
```

## Focused tests

```text
npm --prefix web test -- lib/__tests__/accessibility-render.test.tsx lib/__tests__/route-evidence-map-interaction.test.ts lib/__tests__/score-card-copy.test.ts

 RUN  v4.1.10 C:/sgSHIOK2026/web


 Test Files  3 passed (3)
      Tests  58 passed (58)
   Start at  09:06:26
   Duration  7.64s (transform 2.39s, setup 0ms, import 2.97s, tests 1.45s, environment 2ms)
```

## Full web test

```text
npm --prefix web test

 RUN  v4.1.10 C:/sgSHIOK2026/web

 Test Files  24 passed (24)
      Tests  159 passed (159)
   Start at  09:07:03
   Duration  32.58s (transform 1.66s, setup 0ms, import 3.45s, tests 12.17s, environment 12ms)
```

## Python collect-only

```text
uv run pytest -q --collect-only | Select-Object -Last 1
457 tests collected in 12.86s
```

## Repository integrity

```text
python scripts/check_repo_integrity.py
Write-Output "exit=$LASTEXITCODE"
repo_integrity=ok
exit=0
```

## Evidence path ignore check

```text
git check-ignore -v qa/verification/P619-night-lighting-note-state.md
Write-Output "exit=$LASTEXITCODE"
exit=1
```

## Protected diff

```text
git diff --name-only -- pipeline/config/weights.yaml checksums.json web/public/data qa/releases 'qa/p6_*' 'qa/p7_*' 'qa/p8_*' 'qa/p9_*' 'qa/p10_*' 'qa/p11/d_*'
Write-Output "exit=$LASTEXITCODE"
exit=0
```

## Diff stat before commit

```text
web/app/page.tsx                                         |  9 ++++++++-
web/lib/__tests__/accessibility-render.test.tsx          | 10 ++++++++++
web/lib/__tests__/route-evidence-map-interaction.test.ts |  4 +++-
web/lib/__tests__/score-card-copy.test.ts                |  7 ++++---
4 files changed, 25 insertions(+), 5 deletions(-)
```

## FINDINGS

1. After P618, the night-lighting toggle label was stateful, but the adjacent helper note still always told users to "Switch on" the layer even when it was already on.
2. P619 makes the note state-aware and directly tests both off and on text through the exported helper.
3. This is a user-visible instruction fix only; lamp-post data, map tiles, score values, exported data, protected artifacts, deployment state, and locked weights are unchanged.

## DISAGREEMENTS

1. None.
